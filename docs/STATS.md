<!-- Generated by tools/generate-stats.ts — do not edit. Run: npm run docs:stats -->

# Figures

Everything here is derived from the checked-in data, so the guides can link to
it instead of quoting numbers that go stale. Regenerate with `npm run docs:stats`;
`tests/stats.test.ts` fails if this file drifts from the data.

## The format

|  | Count |
| --- | ---: |
| Format version written for new flows, and the newest readable | v112 |
| Object types in the wire schema (blocks, expressions, values) | 609 |
| Block types (statements) | 410 |
| Serialized fields across all types | 3,971 |
| …of those, gated to a range of format versions | 486 |

A version-gated field is skipped when saving a flow older than its gate, which
is why adding a modern block to an old flow can silently drop arguments.

## Block arguments, by `fieldKind()`

`op: 'obj'` does not mean "any expression": the app casts each argument as it
reads it, so the wrong node type throws inside Automate when the flow loads.
These are the kinds `fieldKind(typeId, field)` reports, over every editable
field of every block type.

| Kind | Fields |
| --- | ---: |
| `expression` | 1,244 |
| `variable` | 486 |
| `integer` | 116 |
| `flag` | 6 |
| `text` | 3 |
| `variable-array` | 3 |
| `number` | 3 |
| `opaque` | 1 |
| `bigint` | 1 |
| `array` | 1 |

1,864 editable fields in total. Anything other than
`expression` needs a specific node type, and ports are excluded here entirely —
they are connections, not arguments, so use `connect()`.

## Blocks by category

| Category | Blocks |
| --- | ---: |
| Connectivity | 70 |
| Camera & sound | 56 |
| Apps | 51 |
| Interface | 38 |
| File & storage | 31 |
| Content | 29 |
| Battery & power | 25 |
| Sensor | 14 |
| Telephony | 14 |
| Messaging | 12 |
| Flow | 12 |
| Date & time | 11 |
| Other | 10 |
| Concurrency | 10 |
| Location | 10 |
| Settings | 9 |
| General | 8 |

## Expressions

|  | Count |
| --- | ---: |
| Expression node types | 149 |
| Functions | 103 |
| Operators | 25 |

## Lint rules

Two independent sources, described in [INTERNALS.md](INTERNALS.md#the-linter).

|  | Count |
| --- | ---: |
| Block classes with a required-argument check in the app | 108 |
| Required-argument candidates found in the app’s code | 140 |
| Fields with enough corpus samples to say anything | 1,039 |
| …set in ≥95% of real blocks, so treated as expected | 248 |
| Flows behind those counts | 798 |
| Minimum samples before a field is considered at all | 20 |
